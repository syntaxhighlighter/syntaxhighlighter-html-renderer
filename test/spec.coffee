chai = require 'chai'
{Renderer} = require '..'
expect = chai.expect

`$ = require('cheerio')` if typeof $ is 'undefined'

describe 'syntaxhighlighter-html-renderer', ->
  describe 'new brush', ->
    brush = null

    before ->

    describe 'instance', ->
      it '...', ->
        console.log $ '<p>test</p>'
